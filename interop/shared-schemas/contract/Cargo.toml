[package]
name = "shared-schemas-contract"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
async-trait = "0.1"
linkrpc = { path = "../../../rust/crates/linkrpc" }
schemars = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
